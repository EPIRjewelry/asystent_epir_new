{
  "name": "epir-art-jewellery",
  "private": true,
  "workspaces": [
    "extensions/*",
    "worker"
  ],
  "scripts": {
    "dev": "shopify app dev",
    "deploy": "shopify app deploy",
    "worker:dev": "wrangler dev",
    "worker:deploy": "wrangler deploy"
  }
}
